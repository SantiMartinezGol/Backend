<div>
    <h1>Listado de Productos en Real Time</h1>
    {{#each productList}}
        <div>
            <p>Id:{{this.id}} Title: {{this.title}} Description:{{this.description}} Code: {{this.code}} Price: {{this.price}} Stock: {{this.stock}}</p>
        </div>
    {{/each}}
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.6.1/socket.io.js"></script>
<script>
    const socket = io();
    socket.emit('message', 'Hola desde el client!!');

   const socketServer = new Server(httpServer); ///// socket

      socketServer.on('connection', socket => {
         console.log('Nuevo cliente conectado');
      });
      socket.on('message', (data) => {
         console.log(data);
      });


</script>

